#include "vector_math.h"



/*template <typename T = float>
Vector<T> sqrt(Vector<T> a) {
    return Vector<T>(sqrt(a.x), sqrt(a.y), sqrt(a.z));
}

template <typename T = float>
Vector<T> operator / (const float &a, const Vector<T> &b) {
    return Vector<T>(a/b.x, a/b.y, a/b.z);
}*/
